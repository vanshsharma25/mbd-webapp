# Solver validation

Before production use, compare canonical mechanisms (pendulum, four-bar linkage, slider-crank) against analytical references and established solver output. Store inputs, expected results, numerical tolerance, and solver version for each validation case.
